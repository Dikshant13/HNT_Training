const colLen = require('../src/length')

describe('testing length of string',function(){
    it('test return length of a string',function(){
        expect(calLength('Dikshant')).toEqual( expected, 8)
    })
})